import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData lightTheme() {
    return ThemeData(
      primarySwatch: Colors.lightBlue,
      scaffoldBackgroundColor: Colors.grey[300], // Color de fondo para toda la aplicación
    );
  }
}
